export type wordToRender = {
    key: number;
    word: string;
    location: string;
    destroyed: boolean;
    dangerWord : boolean;
  };

  export type form = {
    username: string;
    password: string;
}

export type wordRand = {
  word: string;
  key: number;
};